// All features require unit + integration tests before a task is marked complete.
// Integration tests live here — they test end-to-end flows against real or stubbed services.

describe('claude-fitbit integration', () => {
  it.todo('authorize flow saves token and MCP tool returns real data');
  it.todo('trend_summary fetches N days and writes to brian-mem');
});
